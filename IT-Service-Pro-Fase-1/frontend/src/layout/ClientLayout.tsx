import {NavLink,Outlet} from 'react-router-dom';
import {useAuth} from '../auth/AuthContext';
import {Home,TicketPlus,TicketCheck,Building2,Server,FileText,LogOut,LifeBuoy,FolderKanban} from 'lucide-react';
const links=[['/cliente','Inicio',Home],['/cliente/crear-ticket','Crear ticket',TicketPlus],['/cliente/mis-tickets','Mis tickets',TicketCheck],['/cliente/empresa','Mi empresa',Building2],['/cliente/proyectos','Proyectos',FolderKanban],['/cliente/activos','Activos',Server],['/cliente/documentos','Documentos',FileText]] as const;
export function ClientLayout(){const{user,logout}=useAuth();return <div className="min-h-screen bg-slate-50 text-slate-900 dark:bg-slate-950 dark:text-white"><header className="sticky top-0 z-40 border-b bg-white/90 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/90"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3 sm:px-6"><NavLink to="/cliente" className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-gradient-to-br from-blue-600 to-cyan-500 text-white shadow-lg"><LifeBuoy size={20}/></span><div><b className="text-lg font-black tracking-tight">IT Service Pro</b><p className="text-[10px] font-bold uppercase tracking-[.2em] text-slate-400">Portal empresarial</p></div></NavLink><nav className="hidden items-center gap-1 lg:flex">{links.map(([to,l,I])=><NavLink end={to==='/cliente'} key={to} to={to} className={({isActive})=>`flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-bold ${isActive?'bg-blue-600 text-white':'text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-900'}`}><I size={16}/>{l}</NavLink>)}</nav><div className="flex items-center gap-2"><div className="hidden text-right sm:block"><p className="max-w-40 truncate text-sm font-black">{user?.nombre_completo}</p><p className="text-xs text-slate-400">Contacto cliente</p></div><button className="icon-btn" onClick={logout} title="Cerrar sesión"><LogOut size={18}/></button></div></div></header><main className="mx-auto max-w-7xl px-4 py-5 pb-24 sm:px-6 sm:py-8 lg:pb-8"><Outlet/></main><nav className="fixed inset-x-0 bottom-0 z-40 border-t bg-white/95 px-2 pb-[max(.5rem,env(safe-area-inset-bottom))] pt-2 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/95 lg:hidden"><div className="mx-auto grid max-w-lg grid-cols-4">{links.slice(0,4).map(([to,l,I])=><NavLink end={to==='/cliente'} key={to} to={to} className={({isActive})=>`flex flex-col items-center gap-1 rounded-xl py-1.5 text-[10px] font-bold ${isActive?'text-blue-600':'text-slate-500'}`}><I size={20}/><span>{l}</span></NavLink>)}</div></nav></div>}
